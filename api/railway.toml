[build]
buildCommand = "true"

[deploy]
startCommand = "pnpm --filter @care/api build && pnpm --filter @care/api db:migrate && pnpm --filter @care/api start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
